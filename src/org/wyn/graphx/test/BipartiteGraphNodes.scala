package org.wyn.graphx.test

import scala.io.Source._




object BipartiteGraphNodes {

   def main(args: Array[String]): Unit = {
    /*   
     val persons : Array[Node] = fromFile("C:\\Hari\\inputs\\graphx_input\\bipartite\\persons_sample.txt").mkString.split("\n")
     .map { row =>  val cols = row.split(",")
              ( new PersonNode(cols(0), cols(1)))
          }
     val persons : Array[Node] = fromFile("C:\\Hari\\inputs\\graphx_input\\bipartite\\persons_sample.txt").mkString.split("\n")
     .map { row =>  val cols = row.split(",")
              ( new PersonNode(cols(0), cols(1)))
          }
     
   
     
     println(lines(0).getVertexID)    */
   }
}